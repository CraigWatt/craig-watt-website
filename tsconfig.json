// tsconfig.json (repo root)
{
  "extends": "./tsconfig.base.json",
  "files": [],          // no globs here
  "references": [
    { "path": "apps/nextjs-app" }
    // add other workspace projects here as needed
  ]
}
