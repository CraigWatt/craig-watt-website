import * as react_jsx_runtime from 'react/jsx-runtime';
import { AriaToastRegionProps } from '@react-aria/toast';
import { ToastState } from '@react-stately/toast';
import { ToastRegionVariantProps } from '@heroui/theme';
import { ToastProps, ToastPlacement } from './use-toast.mjs';
import 'tailwind-variants';
import 'react';
import '@heroui/system';
import '@heroui/react-utils';
import 'framer-motion';

interface ToastRegionProps<T> extends AriaToastRegionProps, ToastRegionVariantProps {
    toastQueue: ToastState<T>;
    placement?: ToastPlacement;
    maxVisibleToasts: number;
    toastOffset?: number;
    toastProps?: ToastProps;
}
declare function ToastRegion<T extends ToastProps>({ toastQueue, placement, disableAnimation, maxVisibleToasts, toastOffset, toastProps, ...props }: ToastRegionProps<T>): react_jsx_runtime.JSX.Element;

export { ToastRegion };
